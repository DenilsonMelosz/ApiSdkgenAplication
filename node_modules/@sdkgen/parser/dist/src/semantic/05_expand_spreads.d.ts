import { Visitor } from "./visitor";
import type { Spread } from "..";
import type { AstNode } from "../ast";
import { Field, StructType } from "../ast";
export declare class ExpandSpreadsVisitor extends Visitor {
    processed: Set<StructType>;
    expandSpread(fields: Field[], fieldsAndSpreads: Array<Field | Spread>): void;
    visit(node: AstNode): void;
}
