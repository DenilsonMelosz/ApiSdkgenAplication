import type { AstNode, AstRoot } from "../ast";
export declare class SemanticError extends Error {
}
export declare abstract class Visitor {
    protected root: AstRoot;
    constructor(root: AstRoot);
    process(): void;
    visit(node: AstNode): void;
}
