import { Transformer } from "./transformer";
import type { AstNode } from "../ast";
import { EnumType, StructType } from "../ast";
export declare class GiveStructAndEnumNamesTransformer extends Transformer {
    path: string[];
    names: Map<string, {
        type: StructType | EnumType;
        path: string[];
    }>;
    transform<T extends AstNode>(node: T): T;
}
